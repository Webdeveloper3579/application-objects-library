import { Component, ViewEncapsulation } from '@angular/core';

@Component({
    selector     : 'servicegroup',
    standalone   : true,
    templateUrl  : './servicegroup.component.html',
    encapsulation: ViewEncapsulation.None,
})
export class ServiceGroupComponent
{
    /**
     * Constructor
     */
    constructor()
    {
    }
}
